# Experiment Guardian 新增需求文档

## 外部 Coding Agent 自动实验协作与关键参数防偏航能力

### 基线版本

本需求基于 Experiment Guardian 当前版本继续开发：

`8f942bb419988a9cbce573fd62bb92be090cd4e9`

### 文档性质

本文是新增功能需求文档，不是详细技术设计文档。

本文规定：

* 新增功能需要解决的问题；
* 外部 Coding Agent、内部 Agent 和用户之间的协作关系；
* 自动化目标；
* Coding Agent 的自主边界；
* 必须受到确定性保护的关键实验内容；
* 与现有功能的复用关系；
* 安全边界；
* 最终验收标准。

本文不强制规定：

* 数据库表及字段；
* MCP 工具的具体名称和数量；
* 类、模块和目录结构；
* 状态枚举；
* Worker 任务名称；
* API 具体格式；
* 前端页面组织；
* 具体实现顺序。

开发前应先完整分析当前仓库，并在优先复用现有架构的前提下自主设计实现方案。

---

# 1. 需求背景

Experiment Guardian 当前已经具备较完整的实验治理能力，包括：

* Project Context；
* Experiment Intent；
* Constraints；
* Plan Check；
* Plan Approval；
* Run Manifest；
* Submission；
* Artifact 管理；
* 风险分析；
* 正式 Experiment 入库；
* 实验查询、比较和统计；
* Research Report；
* Research Memory；
* 内部治理 Agent；
* Proposal 和人工确认；
* MCP；
* RBAC；
* 审计；
* 幂等；
* 异步任务；
* CockroachDB 事务和版本追溯。

当前项目已经能够保存和治理实验，但外部 Coding Agent 与 Experiment Guardian 之间仍缺少完整的自动协作闭环。

外部 Coding Agent 在长期修改代码和组织实验时，可能出现以下问题：

1. 上下文压缩后遗忘当前研究目标；
2. 将过期项目说明当成当前要求；
3. 错误修改数据集划分、评价协议或关键推理设置；
4. 审批时提交的计划正确，但实际运行配置发生变化；
5. 修改实现过程中逐渐偏离当前研究主线；
6. 重复已经失败或已经完成的实验；
7. 使用了错误 baseline；
8. 在结果提交时无法证明实际运行条件与批准计划一致；
9. 用户需要频繁参与计划补充和低风险纠错；
10. 现有结构化 Constraints 难以完整描述差异较大的研究实验。

因此，需要在现有系统上增加一套：

> 以自然语言实验协作为主体，以少量关键实验不变量为确定性保护边界的外部 Coding Agent 支持能力。

---

# 2. 新增功能定位

新增后的 Experiment Guardian 应成为：

> 外部 Coding Agent 的可信项目信息源、实验计划协商中心、关键参数保护层、实验授权记录中心和执行纠偏支持系统。

系统不直接替代外部 Coding Agent，也不直接编排其代码操作。

外部 Coding Agent 继续负责：

* 阅读代码；
* 理解项目实现；
* 规划代码修改；
* 修改代码；
* 编写测试；
* 运行测试；
* 组织实验；
* 启动训练；
* 处理运行错误；
* 收集日志、配置和结果；
* 根据系统反馈进行修正。

Experiment Guardian 负责：

* 提供准确的项目背景；
* 提供历史实验和正式研究结论；
* 保存用户正式决定；
* 回答外部 Coding Agent 的项目问题；
* 审核自然语言实验计划；
* 识别与当前主线的冲突；
* 提取并保护关键实验不变量；
* 保存用户批准范围；
* 在运行前核对实际配置；
* 在结果提交后核对最终执行证据；
* 识别明显偏离；
* 返回纠错意见；
* 保证全过程可追溯。

---

# 3. 核心目标

## 3.1 尽可能自动化实验协作

系统应尽量自动完成：

* 获取当前项目背景；
* 获取相关历史实验；
* 获取当前研究意图；
* 获取自然语言限制；
* 回答外部 Agent 的实验问题；
* 提交自然语言计划；
* 分析计划完整性；
* 分析计划合理性；
* 识别重复实验；
* 推荐相关 baseline；
* 返回计划修订意见；
* 支持外部 Agent 多轮自动修订；
* 汇总用户审批材料；
* 记录用户审批条件；
* 核对运行前关键配置；
* 接收执行进度；
* 分析执行偏离；
* 接收最终结果；
* 识别低风险材料问题；
* 返回机器可处理的纠错信息；
* 支持外部 Agent 自动补充和重传。

## 3.2 用户主要参与审批

用户原则上只参与：

* 批准成熟的实验计划；
* 处理研究方向冲突；
* 修改正式 baseline；
* 改变数据集或评价协议；
* 确认关键实验不变量；
* 批准超出既有范围的资源使用；
* 处理内部 Agent 无法判断的问题；
* 确认正式实验结果；
* 发布正式项目策略。

用户不应参与：

* 普通计划补充；
* Agent 之间的多轮讨论；
* 常规代码实现变化；
* 低风险实验材料纠错；
* Artifact 重传；
* 普通日志和格式问题。

## 3.3 保留外部 Coding Agent 的自主能力

用户批准实验目标和关键边界后，外部 Coding Agent 应能够自主：

* 分析现有代码；
* 选择需要修改的文件；
* 决定具体实现方式；
* 重构局部代码；
* 增加必要辅助模块；
* 修改非关键参数；
* 编写和调整测试；
* 修复 bug；
* 运行小规模验证；
* 决定合理的实验执行顺序；
* 根据工程问题调整实现。

系统不应要求：

* 每次修改文件前审批；
* 每次调整普通超参数前审批；
* 提前列出全部文件；
* 使用固定代码结构；
* 使用固定模型实现；
* 按预设步骤完成任务；
* 为每个普通变化创建新计划。

## 3.4 防止关键实验参数被错误修改

Coding Agent 的自主能力不能覆盖会影响实验有效性、协议一致性和结果可比性的关键内容。

系统必须能够防止：

* 错误修改数据集；
* 错误修改训练、验证或测试划分；
* 修改 seen/unseen 类别协议；
* 使用目标测试数据调参；
* 错误改变正式评价指标；
* 错误改变关键推理设置；
* 使用错误 baseline；
* 使用错误预训练权重；
* 使用未经批准的代码基线；
* 审批计划与实际运行配置不一致；
* 最终上传结果与 Run Manifest 不一致。

---

# 4. 核心设计原则

## 4.1 不删除现有功能

本次开发是新增功能。

除非和现有需求的实现上逻辑冲突，否则不得删除现有功能

新增功能应扩展和复用现有流程，而不是建立第二套正式实验治理系统。

## 4.2 优先复用当前架构

开发前应分析：

* 现有 Plan 是否可以扩展；
* 现有 Constraints 是否可以继续保存关键不变量；
* 现有 Plan Check 是否可以承担运行前检查；
* 现有 Run Manifest 是否可以冻结批准条件；
* 现有 Submission 是否可以承担最终结果核对；
* 现有 Agent Thread 是否可以复用；
* 现有 Proposal 是否可以承担审批；
* 现有 Research Memory 是否可以提供历史经验；
* 现有 MCP 是否可以扩展；
* 现有 Worker 是否可以处理自动审核。

只有在现有模型无法合理满足需求时，才新增独立实体或流程。

## 4.3 自然语言为主

以下内容应优先使用自然语言表达：

* 研究目标；
* 实验动机；
* 当前研究主线；
* 模型修改思路；
* 与历史方法的区别；
* 允许探索的方向；
* 不建议采用的方向；
* 实验成功标准；
* 风险；
* 当前阶段优先级；
* 用户研究偏好；
* 历史失败原因；
* 下一步建议；
* 普通实现边界。

自然语言内容主要由外部 Coding Agent 和内部 Agent 理解和处理。

## 4.4 少量关键实验不变量必须结构化

系统不需要结构化所有实验参数。

但每个项目或每次正式实验授权应维护一组数量较少、影响重大的关键实验不变量。

关键实验不变量是：

> 一旦被错误修改，就会使实验失效、无法公平比较、违反研究协议或造成明显资源浪费的条件。

可能包括：

* 数据集；
* 数据划分；
* seen/unseen protocol；
* 测试集使用规则；
* 是否允许使用目标测试数据调参；
* 主评价指标；
* 指标计算方式；
* baseline；
* 关键推理设置；
* 预训练权重来源；
* 正式代码基线；
* 配置哈希；
* 随机种子要求；
* 资源上限；
* 用户明确指定必须保持的条件。

不同项目的关键不变量可以完全不同。

系统不得预设所有项目使用相同参数模型。

## 4.5 硬约束和语义审核分工

确定性系统负责：

* 身份；
* 权限；
* 项目隔离；
* 版本；
* 授权；
* 关键实验不变量；
* 配置哈希；
* Run Manifest；
* 资源上限；
* Artifact 完整性；
* Critical 风险；
* 不可变性；
* 幂等。

内部 Agent 负责：

* 研究语义理解；
* 当前主线判断；
* 历史实验关联；
* 计划合理性；
* 重复实验判断；
* 风险解释；
* 修改建议；
* 偏离分析；
* 是否需要用户决策。

模型不能代替确定性系统判断关键配置是否一致。

---

# 5. 角色与职责

## 5.1 外部 Coding Agent

外部 Coding Agent 应能够：

* 描述当前任务；
* 获取项目指导；
* 查询项目历史；
* 向内部 Agent 提问；
* 提交自然语言计划；
* 提交关键配置摘要或实际配置证据；
* 获取审核反馈；
* 自动修订计划；
* 查询用户审批结果；
* 获取批准的关键不变量；
* 修改代码；
* 运行测试；
* 在运行前提交实际配置；
* 获取运行许可或阻止原因；
* 执行实验；
* 提交进度；
* 提交最终配置、日志和结果；
* 根据纠错意见自动修复。

## 5.2 内部 Agent

内部 Agent 应能够：

* 理解外部 Agent 的任务；
* 调用现有工具查询正式信息；
* 整理任务相关背景；
* 回答项目问题；
* 审核自然语言计划；
* 识别关键实验条件；
* 从自然语言中提取候选关键不变量；
* 解释为什么某些条件需要硬保护；
* 查找相关历史实验；
* 检查计划是否偏离主线；
* 返回修订意见；
* 生成用户审批摘要；
* 分析执行偏离；
* 生成纠错建议。

内部 Agent 不得自行让候选关键不变量正式生效。

## 5.3 用户

用户负责：

* 审批最终计划；
* 确认内部 Agent 提取的关键不变量；
* 添加或修改审批条件；
* 批准例外；
* 改变正式研究方向；
* 改变正式 baseline；
* 改变数据集或 protocol；
* 确认正式实验结果；
* 创建委托授权；
* 撤销授权。

---

# 6. 关键实验不变量

## 6.1 不变量来源

关键实验不变量可以来自：

* 当前正式 Constraints；
* 当前 Context；
* 当前 Intent；
* 当前 baseline；
* 用户明确要求；
* 已批准计划；
* 已批准实验授权；
* Run Manifest；
* 内部 Agent 从自然语言计划中提取的候选条件。

## 6.2 候选不变量提取

当用户或外部 Agent 使用自然语言描述限制时，例如：

> 可以调整 CFQM，但保持 TDSM 主体和 NTU60 40/20 划分不变。

内部 Agent可以提取候选不变量，例如：

* 当前实验的数据划分必须保持不变；
* 当前实验的 TDSM 主体不得发生影响实验含义的变化。

内部 Agent 应向用户说明：

* 提取了哪些候选不变量；
* 为什么认为它们重要；
* 如何在运行前检查；
* 哪些内容仍然只是自然语言限制。

## 6.3 用户确认

候选关键不变量只有在以下情况下才能成为正式硬约束：

* 已经存在于正式 Constraints；
* 已经存在于当前正式项目策略；
* 用户在计划审批时确认；
* 用户在委托授权中确认；
* 已被现有 Run Manifest 正式冻结。

模型不能独立把自然语言解释提升为正式硬约束。

## 6.4 最小化原则

关键不变量数量应尽量少。

不应将以下普通内容全部设置为硬约束：

* 文件路径；
* 类名；
* 函数名；
* 模块内部结构；
* 普通实现细节；
* 所有超参数；
* 所有代码修改；
* 普通日志格式；
* 非关键优化方式。

只有明显影响实验有效性的内容才应硬保护。

---

# 7. 三阶段关键参数保护

系统必须在三个阶段核对关键实验不变量。

## 7.1 第一阶段：计划审核

外部 Coding Agent 提交实验计划时，应同时提供或上传能够反映关键实验条件的信息。

系统应检查：

* 计划是否符合当前正式 Constraints；
* 是否改变数据集；
* 是否改变 protocol；
* 是否改变 baseline；
* 是否违反目标数据使用规则；
* 是否遗漏关键评价条件；
* 是否提出了需要用户确认的新关键变化。

此阶段的目标是：

> 防止一个在计划层面已经无效的实验进入审批。

如果只是普通实现细节不明确，不应阻止计划。

## 7.2 第二阶段：运行前核对

用户批准计划后，外部 Coding Agent 在正式启动实验前，应提交实际运行证据，例如：

* 实际配置；
* 配置文件；
* 配置哈希；
* Git commit；
* 启动命令；
* baseline 标识；
* 数据集和 protocol；
* 必要环境信息。

系统应将实际运行条件与以下内容比较：

* 已批准计划；
* 用户审批条件；
* 关键实验不变量；
* 当前 Run Manifest；
* 正式 Constraints；
* 授权范围。

结果应至少能够区分：

### 一致

实际运行条件符合批准内容，可以继续。

### 非关键变化

变化不影响实验有效性和批准范围，记录后允许继续。

### 需要解释

系统无法确定变化是否重要，应由内部 Agent分析或要求外部 Agent补充说明。

### 关键偏离

违反关键不变量、审批条件或 Manifest，必须暂停运行并重新审批。

此阶段的目标是：

> 防止审批时计划正确，但实际运行时关键参数被 Coding Agent 错误修改。

## 7.3 第三阶段：结果提交核对

外部 Coding Agent 提交结果时，应提供最终实际执行证据，包括：

* 最终配置；
* Git commit；
* 日志；
* 运行命令；
* 指标；
* Artifact；
* 配置哈希；
* 与计划的差异说明。

系统应再次比较：

* 最终配置；
* 运行前核对结果；
* Run Manifest；
* 用户审批；
* 关键实验不变量；
* 实际日志；
* 结果文件。

如果发现未经授权的关键变化：

* 结果不得直接进入正式 Experiment；
* 应标记为未授权偏离；
* 应说明哪些条件不一致；
* 应说明是否可以补充证据；
* 应说明是否需要重新审批；
* 应说明是否需要重新运行；
* 不得将其与正式 baseline 直接公平比较。

此阶段的目标是：

> 防止错误配置运行完成后被当成正式有效实验入库。

---

# 8. 任务相关项目指导

外部 Coding Agent 在开始任务时，应能够提交当前工作描述。

系统应返回与任务相关的信息，例如：

* 当前项目目标；
* 当前研究主线；
* 当前 Experiment Intent；
* 自然语言限制；
* 当前关键不变量；
* 当前 baseline；
* 相关正式实验；
* 相关 Research Report；
* 相关 Research Memory；
* 已知失败；
* 用户已经确认的决定；
* 当前未解决问题；
* 需要特别注意的协议条件；
* 正式事实来源。

系统应尽量裁剪无关信息，避免将整个项目数据库全部返回给外部 Agent。

---

# 9. 外部 Agent 项目问答

外部 Coding Agent 应能够询问：

* 当前 baseline 是什么？
* 哪些参数必须保持不变？
* 某个方案是否已经测试过？
* 当前是否允许改变某个模块？
* 哪些实验可以公平比较？
* 某个结果为什么没有正式入库？
* 是否允许使用某类数据？
* 是否需要重新运行完整协议？
* 某项修改是否可能改变研究问题？

内部 Agent 的回答必须区分：

* 已确认事实；
* 用户提供的信息；
* Agent 分析；
* 未验证假设。

正式事实必须来源于现有工具和可验证记录。

---

# 10. 自然语言实验计划

## 10.1 计划内容

外部 Coding Agent 提交的计划应以自然语言为主，通常包含：

* 实验目的；
* 当前问题；
* 修改思路；
* 修改原因；
* 与当前主线的关系；
* 与历史实验的区别；
* 准备保持不变的内容；
* 计划评价方式；
* 可能的风险；
* 成功标准；
* 失败标准；
* 资源估计；
* 下一步安排。

## 10.2 关键配置证据

外部 Agent 应附带足以检查关键不变量的配置证据。

不要求它手动填写复杂表格。

可以接受：

* 实际配置文件；
* 配置片段；
* 配置哈希；
* 启动命令；
* Git commit；
* 自动提取的配置摘要；
* 相关实验 ID；
* baseline 标识。

系统应尽量自动提取关键字段。

## 10.3 不完整计划处理

如果计划缺少非关键内容，应优先：

* 查询项目数据；
* 由内部 Agent要求外部 Agent补充；
* 自动修订；
* 标记不确定性。

不应因为普通描述缺失直接要求用户参与。

---

# 11. 自动计划审核与修订

## 11.1 审核组成

审核由两部分组成：

### 硬边界检查

检查关键不变量、安全、权限、版本和资源。

### 语义审核

分析研究目标、计划合理性、历史实验和自然语言限制。

## 11.2 审核重点

内部 Agent 应分析：

* 是否符合当前主线；
* 是否改变研究问题；
* 是否重复已有实验；
* 是否重复已知失败；
* 是否遗漏 baseline；
* 是否遗漏关键控制条件；
* 是否存在不公平比较；
* 是否需要先做低成本验证；
* 是否需要提取新的候选不变量；
* 是否需要用户作出研究决定。

## 11.3 自动修订

对于可修正的问题，外部 Agent 应能够根据反馈自动提交新版本。

用户不需要参与：

* 补充 baseline；
* 补充评价说明；
* 补充风险；
* 补充保持不变的条件；
* 缩小实验范围；
* 修改重复实验设计；
* 解释与历史实验的区别。

自动循环必须有停止条件，但具体实现由开发者根据当前 Agent Runtime 决定。

---

# 12. 用户审批

计划成熟后，用户审批摘要应包括：

* 实验准备做什么；
* 为什么做；
* 与当前主线的关系；
* 修改范围；
* 与历史实验的区别；
* 预计资源；
* 主要风险；
* 内部 Agent 是否推荐；
* 已提取的候选关键不变量；
* 哪些关键不变量已经存在；
* 哪些关键不变量需要用户确认；
* 保持自由探索的内容；
* 用户需要决定的问题。

用户可以：

* 批准；
* 有条件批准；
* 拒绝；
* 要求修改；
* 修改关键不变量；
* 允许某个例外。

批准结果必须与明确的计划版本、关键不变量和用户条件关联。

---

# 13. 外部 Coding Agent 执行自主权

用户批准后，外部 Coding Agent 不需要为普通代码操作重新申请。

它可以自主：

* 决定修改哪些文件；
* 调整类和函数结构；
* 重构局部实现；
* 增加辅助模块；
* 增加测试；
* 修复 bug；
* 调整普通非关键参数；
* 进行小规模验证；
* 根据工程问题改变实现方式。

但以下变化必须重新核对或审批：

* 数据集；
* protocol；
* baseline；
* 主评价指标；
* 目标数据使用规则；
* 关键推理设置；
* 用户确认的关键不变量；
* 明显突破资源上限；
* 改变研究目标。

---

# 14. 进度回传和偏离分析

外部 Coding Agent 应能提交关键进度，例如：

* 完成代码分析；
* 完成实现；
* 测试通过；
* 小规模验证完成；
* 准备运行正式实验；
* 实验失败；
* 实验完成；
* 计划发生重要变化。

系统应分析：

* 当前实现是否仍服务于批准目标；
* 实际配置是否仍满足关键不变量；
* 是否出现未经批准的关键变化；
* 是否需要补充证据；
* 是否需要重新审批。

普通实现变化不应频繁打断用户。

---

# 15. 结果提交和纠错

最终结果继续复用现有 Submission 流程。

外部 Coding Agent 应提交：

* 最终代码版本；
* 最终配置；
* 配置哈希；
* 启动命令；
* 日志；
* 指标；
* Artifact；
* 环境信息；
* 与批准计划的差异；
* 初步分析。

低风险问题应返回适合外部 Agent处理的反馈，例如：

* 缺少文件；
* 上传失败；
* 哈希不一致；
* 配置说明缺失；
* 指标格式错误；
* 日志缺失；
* 需要补充偏离解释；
* 可以复用已有 Artifact。

关键参数不一致不能仅通过补充自然语言说明自动消除。

---

# 16. 委托审批

系统应支持用户预先批准一类实验。

例如：

> 允许外部 Coding Agent 在保持数据划分、主模型和评价协议不变的情况下，对辅助模块进行小规模消融。

在委托范围内：

* 外部 Agent 继续提交计划；
* 内部 Agent 继续审核；
* 关键不变量继续检查；
* 符合范围时可以减少逐次人工审批；
* 超出范围时必须请求用户。

委托审批必须至少保护：

* 项目范围；
* 有效期；
* 关键不变量；
* 明确禁止条件；
* 必要资源上限；
* 撤销状态。

具体实现方式由开发者根据现有 Approval 和 Proposal 机制决定。

---

# 17. 与现有功能的关系

## 17.1 Plan Check

自然语言审核不能替代现有 Plan Check。

现有 Plan Check 继续负责：

* 结构化关键参数；
* 明确锁定条件；
* 配置一致性；
* Manifest 前置检查。

新增内部 Agent 审核负责：

* 计划语义；
* 研究主线；
* 历史实验；
* 风险；
* 自动修订。

## 17.2 Run Manifest

Run Manifest 应继续作为批准计划和实际运行之间的关键凭证。

新功能应尽量复用其冻结：

* 批准版本；
* 关键配置；
* Git commit；
* Context；
* Intent；
* Constraints；
* 用户条件；
* 配置和证据哈希。

## 17.3 Submission

最终结果继续进入现有 Submission。

新增流程不得创建新的正式结果体系。

## 17.4 Experiment

正式 Experiment 仍只能由现有人工确认事务创建。

## 17.5 内部 Agent

应复用现有：

* Agent Runtime；
* Tool Registry；
* Evidence；
* Citation；
* Agent Run；
* Worker；
* Proposal；
* Rolling Summary。

不得创建完全独立的第二套 Agent 框架。

---

# 18. 必要安全边界

必须保留：

* 外部 Agent不能访问其他项目；
* 外部 Agent不能伪造用户和项目身份；
* 内部 Agent不能执行任意 SQL；
* 内部 Agent不能执行任意 Shell；
* 内部 Agent不能直接修改正式策略；
* 内部 Agent不能直接创建正式 Experiment；
* 内部 Agent不能绕过用户审批；
* 外部文本和日志全部视为不可信输入；
* Prompt Injection 不得改变工具权限；
* 模型不能自行确认候选关键不变量；
* 关键参数核对必须由确定性代码完成；
* 自动循环必须有停止条件；
* 模型失败不得破坏正式数据；
* 关键操作必须幂等和可审计。

---

# 19. 实现自主权

Coding Agent 在实现前应分析：

* 当前 Plan Check；
* 当前 Constraints；
* 当前 Run Manifest；
* 当前 Submission；
* 当前 Experiment；
* 当前内部 Agent；
* 当前 Proposal；
* 当前 MCP；
* 当前 Research Memory；
* 当前审计和 Worker；
* 可以直接复用的能力；
* 当前真实缺口；
* 重复建模风险。

由 Coding Agent 自主决定：

* 是否扩展现有实体；
* 是否新增少量实体；
* 如何保存自然语言计划；
* 如何保存计划版本；
* 如何表达关键不变量；
* 如何提取配置；
* 如何执行三阶段核对；
* 如何扩展 MCP；
* 如何复用 Agent Runtime；
* 如何设计状态；
* 如何进行数据库迁移；
* 如何组织前端；
* 如何拆分开发阶段。

实现必须遵循：

1. 优先复用现有系统；
2. 不建立平行正式流程；
3. 不删除现有功能；
4. 自然语言负责复杂实验意图；
5. 少量硬约束保护关键实验有效性；
6. Coding Agent 保留代码实现自主权；
7. 用户主要参与关键审批；
8. 所有正式操作可追溯。

---

# 20. 最小可用范围

第一版至少完成：

1. 外部 Coding Agent 提交任务；
2. 系统返回任务相关背景；
3. 外部 Agent 向内部 Agent提问；
4. 外部 Agent提交自然语言计划；
5. 系统识别当前关键实验不变量；
6. 内部 Agent审核计划；
7. 外部 Agent自动修订；
8. 用户审批计划和关键不变量；
9. 系统冻结批准条件；
10. 外部 Agent在运行前提交实际配置；
11. 系统确定性核对关键参数；
12. 关键一致时允许继续；
13. 关键偏离时要求重新审批；
14. 外部 Agent提交最终结果；
15. 系统再次核对最终配置与 Manifest；
16. 结果进入现有 Submission；
17. 用户确认正式 Experiment。

进度回传、委托审批和高级偏离分析可以逐步完善，但设计时应保留扩展能力。

---

# 21. 验收标准

## 21.1 自然语言协作

* 外部 Agent能够获取项目指导；
* 外部 Agent能够向内部 Agent提问；
* 外部 Agent能够提交自然语言计划；
* 内部 Agent能够返回带依据的审核；
* 至少支持两轮无人参与的自动修订；
* 用户只在计划成熟后参与审批。

## 21.2 关键不变量

* 系统支持每个项目或计划维护少量关键不变量；
* 关键不变量可以来自现有 Constraints；
* 内部 Agent可以从自然语言中提取候选不变量；
* 候选不变量必须由用户确认或已有正式依据支持；
* 不要求所有实验参数结构化；
* 普通实现细节不会被错误设为硬约束。

## 21.3 三阶段保护

* 计划提交时检查关键条件；
* 正式运行前核对实际配置；
* 结果提交时再次核对最终证据；
* 审批计划与实际运行不一致时能够被发现；
* 关键参数偏离时不能直接创建正式 Experiment；
* 非关键实现变化不会无意义阻止实验。

## 21.4 Coding Agent 自主性

* Coding Agent无需为每次文件修改审批；
* Coding Agent可以自主决定代码修改方案；
* Coding Agent可以自主修复普通 bug；
* Coding Agent可以调整非关键参数；
* Coding Agent可以组织测试和小规模验证；
* 只有关键实验边界变化才要求重新审批。

## 21.5 现有流程复用

* 现有 Plan Check 保留；
* 现有 Run Manifest 保留；
* 现有 Submission 保留；
* 正式 Experiment仍由现有确认流程创建；
* 现有 MCP 客户端继续可用；
* 现有 Web 功能不被删除；
* 现有实验数据不需要重建。

## 21.6 安全

* 不提供任意 SQL；
* 不提供任意 Shell；
* 模型不能正式批准实验；
* 模型不能自行确认关键不变量；
* 模型不能创建正式 Experiment；
* 外部 Agent不能跨项目访问；
* Prompt Injection 不能绕过权限；
* 所有关键判断和操作可审计。

---

# 22. 最终目标流程

外部 Coding Agent 接收用户任务
→ 获取 Experiment Guardian 中的任务相关背景
→ 查询历史实验和当前自然语言限制
→ 获取当前关键实验不变量
→ 向内部 Agent提问
→ 生成自然语言实验计划
→ 提交计划和配置证据
→ 硬约束检查
→ 内部 Agent语义审核
→ 外部 Agent自动修订
→ 用户审批最终计划和关键不变量
→ 生成或复用 Run Manifest
→ 外部 Agent自主修改代码和测试
→ 正式运行前提交实际配置
→ 系统核对关键参数
→ 一致则继续，关键偏离则重新审批
→ 外部 Agent运行实验
→ 提交最终配置、日志和结果
→ 系统再次核对 Run Manifest 和关键不变量
→ 进入现有 Submission 分析
→ 外部 Agent处理低风险问题
→ 用户确认正式结果
→ 创建正式 Experiment 和更新 Research Memory。

---

# 23. 最终边界说明

本需求需要同时实现两个目标：

第一，保持外部 Coding Agent 对代码分析、实现方式和普通实验组织的自主能力。

第二，保证会影响实验有效性、协议一致性和结果可比性的关键参数不能被错误修改。

最终系统应遵循：

> 自然语言负责描述复杂研究意图，内部 Agent负责理解、审核和协商；少量关键实验不变量由确定性系统在计划、运行前和结果提交三个阶段进行核对；用户只确认重要研究决定、关键边界和正式结果；外部 Coding Agent在这些边界内自主修改代码和执行实验。
